// Graph component usually doesn't have specific CSS file in the list I saw earlier,
// but let's check if there is one or if it uses main.css
import './js/GraphRenderer.js';
import './js/GraphComponent.js';
